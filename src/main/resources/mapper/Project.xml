<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.employeesystem.mapper.ProjectMapper">
    <select id="getAll" resultType="project">
        select * from project;
    </select>
    <select id="existsByName" parameterType="String">
        select count(*) from project where project_name = #{projectName}
    </select>
    <select id="existsByNameExceptId">
        select count(*) from project where project_name = #{name} and project_id != #{id}
    </select>
    <select id="getProjectById" parameterType="Long" resultType="project">
        select * from project where project_id = #{id}
    </select>


    <insert id="insert" useGeneratedKeys="true" keyProperty="projectId">
        insert into project(project_name, start_date, end_date, status, description) values(
          #{projectName},#{startDate},#{endDate},#{status},#{description}
        )
    </insert>
    <update id="update" parameterType="project">
        update project
        <set>
            <if test="projectName != null">project_name=#{projectName},</if>
            <if test="startDate != null">start_date=#{startDate},</if>
            <if test="endDate != null">end_date=#{endDate},</if>
            <if test="status!= null">status=#{status},</if>
            <if test="description!= null">description=#{description},</if>
        </set>
        where project_id = #{projectId}
    </update>
</mapper>